import '../styles/css/LoadingComponent.css'; // Import the CSS file

const LoadingComponent = () => {
    return (
        <div className="loader-container sbox">
            <div className="loader"></div>
        </div>
    );
};

export default LoadingComponent;